import Container from "@mui/material/Container";
import MainContainer from "../components/MainContainer";

const Home = () => {
  return (
    <Container maxWidth="12px" sx={{ paddingTop: 5 }}>
      <MainContainer />
    </Container>
  );
};

export default Home;
